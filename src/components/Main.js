import React from "react";
import Header from "./Header";

function Main() {
  return (
    <main className="w-screen min-h-screen flex items-center justify-between flex-col bg-primary">
      <Header />
    </main>
  );
}

export default Main;
